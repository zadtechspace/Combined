import { Button } from "@mui/material"
import MUIcard from "./MUIcard"

const MUI = () => {
  return (
    <div>
        <Button style={{background:"red", color:"black"}}>Hello world</Button>
        <MUIcard/>
    </div>
  )
}

export default MUI